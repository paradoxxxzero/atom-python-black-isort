# python-black-isort package

Run isort then black on the current python file. (Order can be configured)

Default keybinding for formatting a python file: `Ctrl-Shift-C`.

This package needs python black and isort.
